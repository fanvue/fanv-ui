{/* GENERATED by fanv-ui scripts/generate-docs.mjs — do not edit. Source: fanvue/fanv-ui */}

## Default

<Frame>
  <iframe src={"https://main--697a1b6dd4dad73ee9c0e5f5.chromatic.com/iframe.html?id=components-select--default&viewMode=story&shortcuts=false&singleStory=true&globals=theme:light"} width="100%" height="200" style={{border: "none", borderRadius: "8px"}} loading="lazy" title="Select — Default" />
</Frame>

```tsx
import { Select, SelectContent, SelectItem } from "@fanvue/ui";

const COUNTRIES = [
  { value: "us", label: "United States" },
  { value: "uk", label: "United Kingdom" },
  { value: "ca", label: "Canada" },
  { value: "au", label: "Australia" },
  { value: "de", label: "Germany" },
];

const DefaultContent = () => (
  <SelectContent>
    {COUNTRIES.map((c) => (
      <SelectItem key={c.value} value={c.value}>
        {c.label}
      </SelectItem>
    ))}
  </SelectContent>
);

const args = {
  label: "Label",
  placeholder: "Placeholder Text",
  helperText: "Helper Text",
};

<Select {...args}>
  <DefaultContent />
</Select>
```

## Without Label

<Frame>
  <iframe src={"https://main--697a1b6dd4dad73ee9c0e5f5.chromatic.com/iframe.html?id=components-select--without-label&viewMode=story&shortcuts=false&singleStory=true&globals=theme:light"} width="100%" height="200" style={{border: "none", borderRadius: "8px"}} loading="lazy" title="Select — Without Label" />
</Frame>

```tsx
import { Select, SelectContent, SelectItem } from "@fanvue/ui";

const COUNTRIES = [
  { value: "us", label: "United States" },
  { value: "uk", label: "United Kingdom" },
  { value: "ca", label: "Canada" },
  { value: "au", label: "Australia" },
  { value: "de", label: "Germany" },
];

const DefaultContent = () => (
  <SelectContent>
    {COUNTRIES.map((c) => (
      <SelectItem key={c.value} value={c.value}>
        {c.label}
      </SelectItem>
    ))}
  </SelectContent>
);

const args = {
  "aria-label": "Country",
  placeholder: "Select a country",
};

<Select {...args}>
  <DefaultContent />
</Select>
```

## Full Width

<Frame>
  <iframe src={"https://main--697a1b6dd4dad73ee9c0e5f5.chromatic.com/iframe.html?id=components-select--full-width&viewMode=story&shortcuts=false&singleStory=true&globals=theme:light"} width="100%" height="200" style={{border: "none", borderRadius: "8px"}} loading="lazy" title="Select — Full Width" />
</Frame>

```tsx
import { Select, SelectContent, SelectItem } from "@fanvue/ui";

const COUNTRIES = [
  { value: "us", label: "United States" },
  { value: "uk", label: "United Kingdom" },
  { value: "ca", label: "Canada" },
  { value: "au", label: "Australia" },
  { value: "de", label: "Germany" },
];

const DefaultContent = () => (
  <SelectContent>
    {COUNTRIES.map((c) => (
      <SelectItem key={c.value} value={c.value}>
        {c.label}
      </SelectItem>
    ))}
  </SelectContent>
);

const args = {
  label: "Country",
  placeholder: "Select a country",
  fullWidth: true,
};

<Select {...args}>
  <DefaultContent />
</Select>
```

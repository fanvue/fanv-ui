{/* GENERATED by fanv-ui scripts/generate-docs.mjs — do not edit. Source: fanvue/fanv-ui */}

## Default

<Frame>
  <iframe src={"https://main--697a1b6dd4dad73ee9c0e5f5.chromatic.com/iframe.html?id=components-datepicker--default&viewMode=story&shortcuts=false&singleStory=true&globals=theme:light"} width="100%" height="200" style={{border: "none", borderRadius: "8px"}} loading="lazy" title="DatePicker — Default" />
</Frame>

```tsx
import { useState } from "react";
import { DatePicker } from "@fanvue/ui/date-picker";

const DEFAULT_MONTH = new Date(2026, 1);

function Example() {
  const [selected, setSelected] = useState<Date | undefined>();
  return (
    <DatePicker
      mode="single"
      defaultMonth={DEFAULT_MONTH}
      selected={selected}
      onSelect={setSelected}
      onApply={() => console.log("Applied:", selected)}
      onCancel={() => setSelected(undefined)}
    />
  );
}
```

## Single Selection

<Frame>
  <iframe src={"https://main--697a1b6dd4dad73ee9c0e5f5.chromatic.com/iframe.html?id=components-datepicker--single-selection&viewMode=story&shortcuts=false&singleStory=true&globals=theme:light"} width="100%" height="200" style={{border: "none", borderRadius: "8px"}} loading="lazy" title="DatePicker — Single Selection" />
</Frame>

```tsx
import { useState } from "react";
import { DatePicker } from "@fanvue/ui/date-picker";

const DEFAULT_MONTH = new Date(2026, 1);

function Example() {
  const [selected, setSelected] = useState<Date | undefined>(new Date(2026, 1, 15));
  return (
    <DatePicker
      mode="single"
      defaultMonth={DEFAULT_MONTH}
      selected={selected}
      onSelect={setSelected}
      onApply={() => console.log("Applied:", selected)}
      onCancel={() => setSelected(undefined)}
    />
  );
}
```

## Range Selection

<Frame>
  <iframe src={"https://main--697a1b6dd4dad73ee9c0e5f5.chromatic.com/iframe.html?id=components-datepicker--range-selection&viewMode=story&shortcuts=false&singleStory=true&globals=theme:light"} width="100%" height="200" style={{border: "none", borderRadius: "8px"}} loading="lazy" title="DatePicker — Range Selection" />
</Frame>

```tsx
import { useState } from "react";
import { DatePicker } from "@fanvue/ui/date-picker";
import type { DateRange } from "@fanvue/ui/date-picker";

const DEFAULT_MONTH = new Date(2026, 1);

function Example() {
  const [range, setRange] = useState<DateRange | undefined>({
    from: new Date(2026, 1, 9),
    to: new Date(2026, 1, 19),
  });
  return (
    <DatePicker
      mode="range"
      defaultMonth={DEFAULT_MONTH}
      selected={range}
      onSelect={setRange}
      onApply={() => console.log("Applied:", range)}
      onCancel={() => setRange(undefined)}
    />
  );
}
```

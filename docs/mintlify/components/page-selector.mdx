---
title: "PageSelector"
description: "A compact control for stepping through a small, known set of pages one at a time, showing the position as \"current of total\" between a back and forward arrow."
---

import PageSelectorProps from "/snippets/ui/page-selector-props.mdx";
import PageSelectorExamples from "/snippets/ui/page-selector-examples.mdx";

```tsx
import { PageSelector } from "@fanvue/ui";
import type { PageSelectorProps } from "@fanvue/ui";
```

## Examples

<PageSelectorExamples />

## Props

<PageSelectorProps />

{/* Add hand-written guidance below — when to reach for PageSelector, accessibility notes,
    composition patterns. Everything above this line is regenerated by
    scripts/generate-docs.mjs in fanvue/fanv-ui; everything below it is yours. */}

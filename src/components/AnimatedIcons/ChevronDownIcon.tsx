"use client";

/*! Animated icon artwork from lucide-animated (MIT, (c) 2024-2026 pqoqubbw) — https://lucide-animated.com (registry item "chevron-down"). @license MIT */
// Generated by scripts/import-animated-icons.mjs — do not edit by hand.
import type { Transition } from "motion/react";
import { motion } from "motion/react";
import * as React from "react";
import { cn } from "@/utils/cn";
import { ICON_SIZE_CLASS } from "../Icons/iconSizeClass";
import type { IconSize } from "../Icons/types";
import type { AnimatedIconHandle, AnimatedIconProps } from "./types";
import { useIconAnimation } from "./useIconAnimation";

const DEFAULT_TRANSITION: Transition = {
  times: [0, 0.4, 1],
  duration: 0.5,
};

const STROKE_WIDTH: Record<IconSize, number> = {
  16: 2.25,
  24: 1.875,
  32: 1.6875,
};

/** Props for {@link ChevronDownIcon}. See {@link AnimatedIconProps} for the shared shape. */
export type ChevronDownIconProps = AnimatedIconProps;

/** Imperative handle for {@link ChevronDownIcon}, via its `controlRef` prop. */
export type ChevronDownIconHandle = AnimatedIconHandle;

/**
 * Animated ChevronDown icon — the animated twin of `ChevronDownIcon` from
 * `@fanvue/ui`. Plays on hover, or on demand through `controlRef`, and stays
 * still when the user has asked for reduced motion.
 * Renders at sizes 16, 24, or 32 px — the same box and stroke weight as the static icon.
 *
 * @example
 * ```tsx
 * import { ChevronDownIcon } from "@fanvue/ui/animated-icons";
 *
 * <ChevronDownIcon size={24} />
 * ```
 */
export const ChevronDownIcon = React.forwardRef<SVGSVGElement, ChevronDownIconProps>(
  (
    { size = 24, filled: _filled, className, controlRef, onMouseEnter, onMouseLeave, ...props },
    ref,
  ) => {
    const { controls, handleMouseEnter, handleMouseLeave } = useIconAnimation({
      controlRef,
      onMouseEnter,
      onMouseLeave,
    });

    return (
      <svg
        ref={ref}
        aria-hidden="true"
        {...props}
        fill="none"
        stroke="currentColor"
        strokeLinecap="round"
        strokeLinejoin="round"
        viewBox="0 0 24 24"
        strokeWidth={STROKE_WIDTH[size]}
        className={cn(ICON_SIZE_CLASS[size], className)}
        onMouseEnter={handleMouseEnter}
        onMouseLeave={handleMouseLeave}
      >
        <motion.path
          animate={controls}
          d="m6 9 6 6 6-6"
          transition={DEFAULT_TRANSITION}
          variants={{
            normal: { y: 0 },
            animate: { y: [0, 2, 0] },
          }}
        />
      </svg>
    );
  },
);

ChevronDownIcon.displayName = "ChevronDownIcon";

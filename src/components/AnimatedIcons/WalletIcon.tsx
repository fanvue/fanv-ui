"use client";

/*! Animated icon artwork from lucide-animated (MIT, (c) 2024-2026 pqoqubbw) — https://lucide-animated.com (registry item "wallet"). @license MIT */
// Generated by scripts/import-animated-icons.mjs — do not edit by hand.
import type { Variants } from "motion/react";
import { motion } from "motion/react";
import * as React from "react";
import { cn } from "@/utils/cn";
import { ICON_SIZE_CLASS } from "../Icons/iconSizeClass";
import type { AnimatedIconHandle, AnimatedIconProps } from "./types";
import { useIconAnimation } from "./useIconAnimation";

const VARIANTS: Variants = {
  normal: {
    y: "0%",
    rotate: 0,
    transition: {
      duration: 0.3,
      ease: "easeOut",
    },
  },
  animate: {
    y: ["0%", "-12.5%", "0%"],
    rotate: [0, -4, 0],
    transition: {
      duration: 0.55,
      ease: "easeInOut",
      times: [0, 0.45, 1],
    },
  },
};

/** Props for {@link WalletIcon}. See {@link AnimatedIconProps} for the shared shape. */
export type WalletIconProps = AnimatedIconProps;

/** Imperative handle for {@link WalletIcon}, via its `controlRef` prop. */
export type WalletIconHandle = AnimatedIconHandle;

/**
 * Animated Wallet icon — the animated twin of `WalletIcon` from
 * `@fanvue/ui`. Plays on hover, or on demand through `controlRef`, and stays
 * still when the user has asked for reduced motion.
 * Renders at sizes 16, 24, or 32 px — the same box as the static icon.
 *
 * @example
 * ```tsx
 * import { WalletIcon } from "@fanvue/ui/animated-icons";
 *
 * <WalletIcon size={24} />
 * ```
 */
export const WalletIcon = React.forwardRef<SVGSVGElement, WalletIconProps>(
  (
    {
      size = 24,
      filled: _filled,
      className,
      style,
      controlRef,
      onMouseEnter,
      onMouseLeave,
      ...props
    },
    ref,
  ) => {
    const { controls, handleMouseEnter, handleMouseLeave } = useIconAnimation({
      controlRef,
      onMouseEnter,
      onMouseLeave,
    });

    return (
      <motion.svg
        ref={ref}
        aria-hidden="true"
        {...props}
        animate={controls}
        fill="none"
        initial="normal"
        stroke="currentColor"
        strokeLinecap="round"
        strokeLinejoin="round"
        variants={VARIANTS}
        viewBox="0 0 24 24"
        strokeWidth="2"
        className={cn(ICON_SIZE_CLASS[size], className)}
        style={{ transformOrigin: "12px 12px", ...style }}
        onMouseEnter={handleMouseEnter}
        onMouseLeave={handleMouseLeave}
      >
        <path d="M19 7V4a1 1 0 0 0-1-1H5a2 2 0 0 0 0 4h15a1 1 0 0 1 1 1v4h-3a2 2 0 0 0 0 4h3a1 1 0 0 0 1-1v-2a1 1 0 0 0-1-1" />
        <path d="M3 5v14a2 2 0 0 0 2 2h15a1 1 0 0 0 1-1v-4" />
      </motion.svg>
    );
  },
);

WalletIcon.displayName = "WalletIcon";

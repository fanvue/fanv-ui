"use client";

// Generated by scripts/import-animated-icons.mjs — do not edit by hand.
// Source: lucide-animated (MIT, (c) 2024-2026 pqoqubbw) — https://lucide-animated.com (registry item "lock-keyhole").
import { motion } from "motion/react";
import * as React from "react";
import { cn } from "@/utils/cn";
import type { AnimatedIconHandle, AnimatedLegacyIconProps } from "./types";
import { useIconAnimation } from "./useIconAnimation";

/** Props for {@link LockerOnIcon}. See {@link AnimatedLegacyIconProps} for the shared shape. */
export type LockerOnIconProps = AnimatedLegacyIconProps;

/** Imperative handle for {@link LockerOnIcon}, via its `controlRef` prop. */
export type LockerOnIconHandle = AnimatedIconHandle;

/**
 * Animated LockerOn icon — the animated twin of `LockerOnIcon` from
 * `@fanvue/ui`. Plays on hover, or on demand through `controlRef`.
 * Sized via `className`, defaulting to the same `size-6` box as the static icon.
 *
 * @example
 * ```tsx
 * import { LockerOnIcon } from "@fanvue/ui/animated-icons";
 *
 * <LockerOnIcon />
 * ```
 */
export const LockerOnIcon = React.forwardRef<SVGSVGElement, LockerOnIconProps>(
  ({ className, controlRef, onMouseEnter, onMouseLeave, ...props }, ref) => {
    const { controls, handleMouseEnter, handleMouseLeave } = useIconAnimation({
      controlRef,
      onMouseEnter,
      onMouseLeave,
    });

    return (
      <motion.svg
        ref={ref}
        animate={controls}
        fill="none"
        initial="normal"
        stroke="currentColor"
        strokeLinecap="round"
        strokeLinejoin="round"
        strokeWidth="2"
        transition={{
          duration: 1,
          ease: [0.4, 0, 0.2, 1],
        }}
        variants={{
          normal: {
            rotate: 0,
            scale: 1,
          },
          animate: {
            rotate: [-3, 1, -2, 0],
            scale: [0.95, 1.05, 0.98, 1],
          },
        }}
        viewBox="0 0 24 24"
        aria-hidden="true"
        className={cn("size-6", className)}
        onMouseEnter={handleMouseEnter}
        onMouseLeave={handleMouseLeave}
        {...props}
      >
        <circle cx="12" cy="16" r="1" />
        <rect height="12" rx="2" width="18" x="3" y="10" />
        <motion.path
          animate={controls}
          d="M7 10V7a5 5 0 0 1 10 0v3"
          initial="normal"
          transition={{
            duration: 0.3,
            ease: [0.4, 0, 0.2, 1],
          }}
          variants={{
            normal: {
              pathLength: 1,
            },
            animate: {
              pathLength: 0.7,
            },
          }}
        />
      </motion.svg>
    );
  },
);

LockerOnIcon.displayName = "LockerOnIcon";

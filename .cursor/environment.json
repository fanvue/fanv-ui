{
  "$schema": "https://www.cursor.com/schemas/environment.schema.json",
  "install": "corepack enable && corepack prepare pnpm@9.15.4 --activate && pnpm install --frozen-lockfile && pnpm exec playwright install --with-deps chromium",
  "terminals": [
    {
      "name": "storybook",
      "command": "pnpm storybook --no-open -p 6006"
    }
  ]
}

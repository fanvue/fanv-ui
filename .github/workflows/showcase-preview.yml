name: Showcase Preview

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, closed]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: showcase-preview-${{ github.event.number || github.run_id }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy Preview
    if: github.event_name == 'workflow_dispatch' || github.event.action != 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build showcase
        run: pnpm build:showcase
        env:
          VITE_BASE: ${{ github.event_name == 'pull_request' && format('/fanv-ui/pr-{0}/', github.event.number) || '/' }}

      - name: Deploy preview
        if: github.event_name == 'pull_request'
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: dist-showcase
          destination_dir: pr-${{ github.event.number }}
          keep_files: true

      - name: Comment on PR
        if: github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: showcase-preview
          message: |
            ## Showcase Preview

            | Preview | Status |
            |---------|--------|
            | [View Showcase](https://fanvue.github.io/fanv-ui/pr-${{ github.event.number }}/) | ✅ Ready |

  cleanup:
    name: Cleanup Preview
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Remove preview directory
        run: |
          if [ -d "pr-${{ github.event.number }}" ]; then
            git config user.name "github-actions[bot]"
            git config user.email "github-actions[bot]@users.noreply.github.com"
            git rm -rf "pr-${{ github.event.number }}"
            git commit -m "chore: remove showcase preview for PR #${{ github.event.number }}"
            git push
          fi
